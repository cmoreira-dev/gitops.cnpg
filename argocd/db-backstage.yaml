apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg-backstage
  namespace: argocd
spec:
  project: homelab
  source:
    repoURL: https://github.com/cmoreira-dev/gitops.cloudflared.git
    targetRevision: main
    path: kustomize/cloudflared
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudflared
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true