---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cnpg
  description: >-
    CloudNativePG operator plus the per-app Postgres Cluster resources it
    manages (backstage, litellm) — each Cluster co-located in its
    consumer's own namespace.
  tags:
    - kubernetes
    - homelab
    - infrastructure
    - platform-addon
    - database
  annotations:
    github.com/project-slug: cmoreira-dev/gitops.cnpg
    backstage.io/techdocs-ref: dir:.
spec:
  type: platform-addon
  owner: platform-team
  system: homelab-platform
  lifecycle: production
